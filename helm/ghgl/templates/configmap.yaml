apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ghgl.fullname" . }}-config
  labels:
    {{- include "ghgl.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
